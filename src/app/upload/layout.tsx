export const metadata = {
  title: 'Upload Document',
  description: 'Upload a PDF document to be processed by our AI system',
};

export default function UploadLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 